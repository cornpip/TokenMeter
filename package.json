{
  "name": "token_metered",
  "version": "1.0.0",
  "description": "로컬에서 OpenAI API를 사용하여 데이터를 기록합니다. (파일 업로드, 이미지 생성 가능)",
  "main": "index.js",
  "dependencies": {
    "concurrently": "^9.1.2",
    "csstype": "^3.1.3"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "npm install --prefix server && npm install --prefix client",
    "dev": "concurrently \"npm run server --prefix server\" \"npm run dev --prefix client\"",
    "start": "npm run setup && npm run dev"
  },
  "author": "cornpip",
  "license": "ISC"
}
